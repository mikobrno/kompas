# Kopie: zkopíruj tento soubor jako `.env.local` (pro vývoj) nebo `.env` (pro build/CI)
# Vite načítá proměnné s prefixem VITE_ a inlineuje je při buildu.

############################################
# Varianta A) Cloud Supabase (doporučeno)
############################################
# URL projektu ze Supabase konzole (vypadá jako https://<ref>.supabase.co)
VITE_SUPABASE_URL=https://YOUR_PROJECT.ref.supabase.co
# Anon public key z Project Settings → API (nikdy necommituj skutečné klíče do repozitáře)
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY

############################################
# Varianta B) Lokální Supabase (supabase start)
############################################
# VITE_SUPABASE_URL=http://127.0.0.1:54321
# VITE_SUPABASE_ANON_KEY=YOUR_LOCAL_ANON_KEY

# Tipy:
# - Skutečné klíče ukládej pouze do `.env.local` nebo CI secretů, ne do gitu.
# - Service Role key NEDÁVEJ do frontendu (má plná práva). Pokud ho používáš, tak jen na backendu.
